spring:
  datasource:
    url: jdbc:postgresql://localhost/dictionary
    username: postgres
    password: password
  jpa:
    generate-ddl: false
    show-sql: true
    hibernate:
      ddl-auto: update
